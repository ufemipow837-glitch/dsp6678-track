# TMS320C6678 无人机跟踪算法 实验报告

> 工程仓库: https://github.com/ufemipow837-glitch/dsp6678-track

---

## 1. FPGA ↔ DSP 接口定义

### 1.1 FPGA → DSP 输入：`TARGET_500_PACK`

DSP 通过 `DataProcess2DspFunc()` 函数接收 FPGA 送来的每帧点迹数据：

```c
typedef struct {
    unsigned short narw_mtd_noise;   // MTD噪声(dB×100, 例: 10000=100.00dB)
    unsigned short wide_mtd_noise;   // 宽脉MTD噪声
    unsigned short bpn;              // 波位号 0~16（共17波束）
    unsigned short n;                // 本波位有效点迹数量
    int            cpi_cnt;          // CPI帧计数（=DSP内部frameSn）
    unsigned int   deal_n;           // 从核处理次数
    unsigned int   twstas_op;        // 雷达硬件工作模式: 0=TWS, 1=TAS
    unsigned int   pihao;            // 批号
    TARGET_PRI_INFO info[500];       // 最多500个点迹
} TARGET_500_PACK;
```

每个点迹 `TARGET_PRI_INFO` 包含原始参数 + 转换后的球坐标：

| 字段 | 含义 | 单位 |
|------|------|------|
| `conv.r` | 斜距 | m |
| `conv.az` | 方位角 | rad |
| `conv.p` | 俯仰角 | rad |
| `conv.v` | 径向速度 | m/s |
| `conv.snr` | 信噪比 | dB |
| `conv.time` | 绝对时间戳 | ms |
| `conv.rcs` | 雷达散射截面积 | m² |

### 1.2 DSP → 上位机输出：`TRACK_OUTPUT` + `TAS_BEAM_CFG`

```c
// 航迹输出
typedef struct {
    int                       reliable_track_num;   // 可靠航迹数量
    struct TARGETTRACE_RENEW  *tracks;              // 航迹数组指针
} TRACK_OUTPUT;

// 波束引导输出（TAS模式用）
typedef struct {
    int         reliable_track_num;
    MANAGEPARA *beams;   // 每个航迹对应的波束调度参数
} TAS_BEAM_CFG;

int DataProcess2DspFunc(const TARGET_500_PACK *targetPack,
                        TRACK_OUTPUT *trackOutput,
                        TAS_BEAM_CFG *tasCfg);
```

`TARGETTRACE_RENEW` 每个航迹的关键字段：

| 字段 | 类型 | 含义 |
|------|------|------|
| `traceType` | uint32_t | **0=TWS扫描跟踪, 1=TAS精跟**（判断切TAS的唯一标志） |
| `tgtnum` | uint32_t | 目标编号 |
| `range` | float | 斜距 (m) |
| `azi` | float | 方位角 (rad) |
| `ele` | float | 俯仰角 (rad) |
| `X/Y/Z` | float | ECEF直角坐标 (m) |
| `Vel` | float | 径向速度 (m/s)，**正=靠近，负=远离** |
| `Vx/Vy/Vz` | float | 三维速度分量 (m/s) |
| `num_P` | int | 航迹批号 |

---

## 2. 模拟输入数据格式

### 2.1 数据来源

`data_entry.c` 生成仿真数据，被 `main.c` 在 DSP 启动时调用：

```c
data_entry(&target_data[0]);
```

### 2.2 单帧数据结构

```c
// main.c 喂入格式 (每帧)
TARGET_500_PACK test_input_pack = {0};
test_input_pack.bpn      = 波束号 0~16;
test_input_pack.n        = 本波位点数;
test_input_pack.cpi_cnt  = frameSn;
test_input_pack.twstas_op = 0;   // 仿真时始终 TWS

// 每个点迹
test_input_pack.info[0].conv.r   = range_m;
test_input_pack.info[0].conv.az  = azi_rad;
test_input_pack.info[0].conv.p   = ele_rad;
test_input_pack.info[0].conv.v   = velocity_mps;
test_input_pack.info[0].conv.time = mSecond_timestamp;
```

### 2.3 当前仿真场景

| 参数 | 值 | 说明 |
|------|-----|------|
| 扫描方式 | 17 beam 顺序扫 | 每波位 164ms |
| 一圈时间 | 17 × 164ms ≈ 2.8s | |
| 总帧数 | 1300 帧 | ≈ 213s ≈ 76 圈扫描 |
| 目标速度 | Vr = +12 m/s | **靠近雷达** |
| 起始距离 | ~2633 m | |
| 结束距离 | ~111 m | |
| 出现波束 | 固定方位波束 | 每圈只在特定 beam 被扫到 |
| 中间丢失 | 无（每圈稳定扫到） | |

---

## 3. 三点建立航迹（航迹起始）

### 3.1 主处理流程

```
每帧 DataProcess2DspFunc() 调用链:

  data_entry() 或 FPGA → TARGET_500_PACK
        │
        ▼
  坐标转换 (球坐标 → ECEF xyz)
        │
        ▼
  track_asso()     ← 点迹与已有航迹关联
        │
        ├─ 关联成功 → IMM卡尔曼滤波测量更新
        └─ 关联失败 → 暂存 temp_track
        │
        ▼
  track_initial()  ← 从 temp_track 建立新航迹
        │
        ▼
  track_predict()  ← IMM预测下帧位置
        │
        ▼
  track_die()      ← 超过160帧未更新则消亡
        │
        ▼
  should_switch_to_tas() → tas_switch_flag[]
        │
        ▼
  tas_predict()    ← 切TAS后用弹道模型预测
        │
        ▼
  输出 TRACK_OUTPUT + TAS_BEAM_CFG
```

### 3.2 三点起始算法

**文件**: `track_initial.c`

每帧最多扫描 N-1 个帧差组合，寻找三点共线候选：

```c
calculate_track_quality(pos1, pos2, pos3, dt)
// 用三点算两段速度矢量 v12, v23
// quality = (速度方向一致性 + 速度大小比) / 2
// quality > 0.7 且 转角 < 90° → 合格新航迹
```

| 门限 | 值 | 含义 |
|------|-----|------|
| `TRACK_QUALITY_THRESHOLD` | 0.7 | 三点品质最低值 |
| `VELOCITY_CONSISTENCY_THRESHOLD` | 0.3 | 速度方向余弦下限 |
| `ANGLE_CHANGE_THRESHOLD` | 90° | 最大允许转弯角 |
| `INIT_SIGMA_A` | 0.3 m² | 位置初始化方差 |
| `MAX_NEW_TRACKS_PER_FRAME` | 20 | 每帧最多启批新航迹 |

### 3.3 航迹关联门限

**文件**: `track_asso.c`

```c
gate = PRE_GATE_DISTANCE + T_asso * 150.0f;    // 距离门
PRE_GATE_DISTANCE = 1200m;                      // 基础距离门
TRACK_ASSO_TH = 50.0f;                          // 马氏距离门限
```

**注意**: 无人机轨迹在 TWS 扫描模式下，每圈只有 2~3 次被扫到，跨圈时间差大（最大 ≈ 2.8s）。距离门 = 1200 + 2.8×150 = **1620m**，足以覆盖跨波束距离跳变。

---

## 4. 可靠航迹预测精度验证

### 4.1 滤波器类型

| 模式 | 滤波器 | 文件 |
|------|--------|------|
| TWS 扫描 | IMM (交互多模型) 3模型 CV+CA+CT | `kalman.h`, `track_predict.c` |
| TAS 精跟 | 弹道模型 + IMM | `tas_predict.c` |

状态向量 `X[6]` (ECEF): `[x, vx, y, vy, z, vz]ᵀ`

### 4.2 验证方法

**方法 A: 仿真数据内校验（当前可用）**

`main.c` 输出同时含 "测量值" 和 "滤波值"——对比 `range` 变化的平滑度：

```c
// 输出示例
-> Track[0]: Az=13.65deg El=2.39deg R=2563.3m V=9.3m/s [TWS tt=0]
-> Track[0]: Az=13.46deg El=2.56deg R=2562.7m V=8.9m/s [TWS tt=0]
```

**验证指标**:
- 速度估计平滑性：连续帧 Vel 变化应 < 2m/s
- 距离估计单调性：靠近场景 range 应单调递减
- 丢失帧外推：连续 predict_flag > 0 的帧，range 漂移 < 50m

**方法 B: 实测数据回放**

FPGA 接入后，在 DSP 端同步记录：
1. FPGA 原始点迹 (range, azi, ele)
2. DSP 输出航迹 (X, Y, Z)

对比预测位置 vs 实际点迹的误差分布，画 CEP 图。

### 4.3 可接受的预测误差

| 场景 | 最大丢失帧数 | 最大可接受距离误差 |
|------|-------------|------------------|
| TWS 扫描 (每圈2~3次update) | 160帧 ≈ 26s | 100m |
| TAS 精跟 (连续update) | 不限 | 20m |

---

## 5. TWS → TAS 切换机制

### 5.1 物理背景

| 模式 | 雷达行为 | 目标被扫频率 | 跟踪精度 |
|------|---------|-------------|---------|
| **TWS** (边跟踪边扫描) | 17波束轮流扫，每波束164ms | 每圈只扫到 2~3 次 | 低 |
| **TAS** (跟踪扫描) | 波束锁定目标，驻留跟踪 | 连续跟踪 | 高 |

无人机靠近到一定距离后，必须切 TAS 才能保证跟踪连续性。

### 5.2 切换决策逻辑

**文件**: `track.c` → `should_switch_to_tas()`

```
帧 0: track_update_flag=0 (没扫到) → 滑窗不变，return 0
帧 1: track_update_flag=1 (扫到!)
      gap = frameSn - last_update_frameSn
      if(gap > 17) { upd_in_window = 1 }     // 距上次超过一圈 → 重置
      else         { upd_in_window++ }
      last_update_frameSn = frameSn

门限检查 (按顺序):
  ① upd_in_window >= 3   ?  // 窗口内累计3次扫到
  ② confidence   >= 0.85 ?  // 航迹品质够
  ③ Vr 方向 + range 距离  ?  // 见下表
```

#### Vr 方向决策表

Vr 定义: `Vr = (x*vx + y*vy + z*vz) / range`

> ⚠️ Vr 与输出 `track_renew.Vel` 符号**相反**
> - Vr > 0 → range 增大 → 目标**远离**（Vel < 0）
> - Vr < 0 → range 减小 → 目标**靠近**（Vel > 0）

| 目标方向 | Vr | 切换条件 |
|---------|-----|---------|
| 远离 | > 0 | 窗口累计够 3 次 → 直接切 TAS（无距离门槛） |
| 靠近 | < 0 | 窗口累计够 3 次 **且** range ≤ 2500m → 切 TAS |

### 5.3 滞回锁防反复切换

```c
struct reliable {
    int tas_latched;   // 0=未切TAS, 1=已切TAS且锁死
    float drone_clean_range;  // 最后一次成功关联的range
};
```

**锁置位**: 切 TAS 时 `tas_latched = 1`，之后**不再做门限检查**

**锁解锁** (只在同时满足时解锁):
1. `range > 2 × 2500m = 5000m`
2. `Vr > 0` (目标重新远离)
3. 解锁后重新走窗口累计流程

**效果**: 切 TAS 后一直保持到目标飞走 → 避免在 2500m 边界反复抖动。

### 5.4 关键参数位置

| 参数 | 值 | 位置 |
|------|-----|------|
| 默认切换距离门槛 | 2500 m | `track.c:26` `#define DEFAULT_TAS_SWITCH_RANGE` |
| 窗口帧数 | 17 | `track.c:21` `#define TAS_WINDOW_FRAMES` |
| 窗口内切换所需 update 次数 | 3 | `track.c:22` `#define TAS_WINDOW_THRESHOLD` |
| 置信度门限 | 0.85 | `track.c:25` `#define TAS_CONFIDENCE_THRESHOLD` |
| 滞回解锁门槛 | 2500 × 2 = 5000 m | `track.c:81` `tas_switch_range * 2.0f` |
| 上位机可配置 | `tas_switch_range` 字段 | `include/struct.h:46` `struct debugger_track` |

### 5.5 上位机如何改切换距离

```c
// 方法 1: 代码直接改 #define (编译时)
#define DEFAULT_TAS_SWITCH_RANGE 2500.0f   // 改成你要的值

// 方法 2: 上位机接口 (运行时, 不重编译)
TRACK_CONFIG_PARAM param = {0};
// 设置
param.tas_switch_range = 3000.0f;   // 3km 就切
TrackSetParam(&param);

// 读取当前值
TrackGetParam(&param);
float cur = param.tas_switch_range;
```

---

## 6. 切 TAS 验证方法

### 6.1 仿真验证（当前可用）

**编译运行后观察输出**:

```
-> Track[0]: Az=13.65deg El=2.39deg R=2563.3m V=9.3m/s [TWS tt=0]
...
-> Track[0]: Az=13.84deg El=2.98deg R=2498.0m V=12.3m/s [TAS tt=1]  ← 切了!
-> Track[0]: Az=13.97deg El=3.00deg R=2493.7m V=12.8m/s [TAS tt=1]
```

**判断标准**:
- `traceType` 从 0 变 1
- 变化时刻的 `range` 应接近 2500m
- 一旦变 1 就一直保持 1（滞回锁）

**实测结果**（当前仿真）:
```
首次切 TAS range = 2498.0m  ✅ 正好跨过 2500m 门槛
TAS 持续到 R=111m           ✅ 全程保持
TAS-DENY 总计:
  2x upd_in_window=1 < 3
  2x upd_in_window=2 < 3
  其余 confidence 启动期正常波动
窗口累计正常，很快凑齐 3 次
```

### 6.2 CCS Watch 验证

在 CCS Debug 模式下直接 Watch 内部变量：

```
Watch 窗口输入:
  tas_switch_flag[0]          → 直接看 0/1
  reliable_track[0].tas_latched → 滞回锁状态
  reliable_track[0].predict_flag → 连续外推次数
  target_data_3[0].frameSn    → 当前帧号
```

**traceType vs tas_switch_flag 关系**:
```
traceType = tas_switch_flag[j] ? 1 : 0;
// 完全等价，traceType 就是编码后的 tas_switch_flag
```

### 6.3 实机 FPGA 对接验证

| 步骤 | 操作 | 观察点 |
|------|------|--------|
| 1 | FPGA 初始化 twstas_op=0 | DSP 收到后 `workMode=0`，全程 TWS |
| 2 | 无人机出现 range > 2500m | DSP 输出 `traceType=0` |
| 3 | 无人机飞到 range ≤ 2500m | DSP 应在窗口累计 3 次 update 后输出 `traceType=1` |
| 4 | 上位机收到 `traceType=1` | 通知 FPGA 切换 TAS 波束调度 |
| 5 | FPGA 切 TAS 后 twstas_op=1 | DSP 输入模式变化，tas_predict() 接管 |
| 6 | 无人机远离 range > 5000m | 滞回锁解锁，`traceType` 应从 1 变回 0 |

### 6.4 调试诊断开关

当前代码带了 `[TAS-DENY]` 诊断打印，切不进去时直接告诉你原因：

```
[TAS-DENY] upd_in_window=1 < 3      → 窗口累计不够 (调 TAS_WINDOW_THRESHOLD)
[TAS-DENY] confidence=0.330 < 0.85  → 航迹品质差 (启动期正常)
[TAS-DENY] ...                        → 看具体原因
```

---

## 7. 系统参数速查表

### 7.1 雷达参数

| 参数 | 值 | 位置 |
|------|-----|------|
| 波束数 | 17 | bpn 0~16 |
| 波位驻留时间 | 164 ms | `track_debugger.beam_time` |
| 一圈扫描时间 | ≈ 2.8 s | 17 × 164ms |
| 每帧处理时间 | ≈ 0.1~1.3 ms | main.c Cycle 计数 |
| 最大同时航迹数 | 5 | `MAX_SIMULTANEOUS_TRACKS` |
| 航迹消亡时间 | 160帧 ≈ 26s | `MAX_MISSING_FRAMES_SEARCH` |

### 7.2 航迹起始参数

| 参数 | 值 | 位置 |
|------|-----|------|
| 起始品质门限 | 0.7 | `track_initial.c:18` |
| 关联马氏距离 | 50 | `data_process_func.c:172` |
| 关联距离门 | 1200m + T×150 | `track_asso.c:22,139` |
| 速度范围 | 2~20 m/s | `track_debugger.Vmin/Vmax` |
| 方位范围 | ±90° | `track_debugger.azi_down/up` |
| 俯仰范围 | -5°~60° | `track_debugger.ele_down/up` |
| 距离范围 | 100~5000m | `track_debugger.range_down/up` |

### 7.3 TAS 切换参数

| 参数 | 值 | 位置 |
|------|-----|------|
| 切换距离门槛 | 2500 m | `track.c:26` |
| 窗口帧数 | 17 | `track.c:21` |
| 窗口累计次数 | 3 | `track.c:22` |
| 置信度门限 | 0.85 | `track.c:25` |
| 滞回解锁距离 | 5000 m | `track.c:81` |
| 上位机可配 | `tas_switch_range` | `struct.h:46` |

---

## 8. 关键代码文件索引

| 文件 | 职责 |
|------|------|
| `main.c` | 仿真入口，喂数据 + 打印输出 |
| `data_entry.c` | 生成模拟点迹数据 |
| `data_process_func.c/h` | 顶层函数 `DataProcess2DspFunc` + FPGA 接口结构体 |
| `track.c` | 主处理流程 + TAS 切换判定 + traceType 输出 |
| `track_asso.c` | 点迹-航迹关联 |
| `track_initial.c` | 三点航迹起始 |
| `track_predict.c` | IMM 预测 |
| `tas_predict.c` | TAS 弹道预测 |
| `track_die.c` | 航迹消亡判定 |
| `include/struct.h` | 全部结构体定义 |
| `include/global_variable.h` | 全局变量 extern 声明 |

---

## 9. 历史问题与修复记录

| 问题 | 原因 | 修复 |
|------|------|------|
| 全是 TWS，永远不切 | 旧逻辑 `TAS_SWITCH_THRESHOLD=6` 要求连续 6 帧 update，TWS 扫描下最多凑 3 次就被清零 | 改用滑窗累计：`upd_in_window[]` 窗口过期才重置 |
| traceType ≥ 100 判 TAS 有歧义 | `traceType = 100 + predict_flag`，而 TAS 成功 update 后 pf=-1 → traceType=99 < 100 | 改成 `traceType = tas_switch_flag ? 1 : 0`，零歧义 |
| Vr 方向注释反了 | 注释写"靠近为正"但公式实际是"远离为正" | 修正注释 + 滞回锁解锁条件 (Vr<0 → Vr>0) |
| `target_data_3` 前向声明缺失 | `should_switch_to_tas` 在 L60 用到 L246 才定义的 static 变量 | 在 L60 加 `static struct target_track target_data_3[cpi_num];` 暂定定义 |
